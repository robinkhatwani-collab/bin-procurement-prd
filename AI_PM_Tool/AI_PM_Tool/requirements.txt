# AI PM Tool — Python dependencies
# Install with: pip install -r requirements.txt

# Core server
openpyxl>=3.1.0          # Excel parsing for project upload

# RAG engine
beautifulsoup4>=4.12.0   # HTML parsing & text extraction
chromadb>=0.5.0          # Local vector store (bundles ONNXMiniLM embeddings)

# Claude generation
anthropic>=0.40.0        # Claude API (set ANTHROPIC_API_KEY env var)
